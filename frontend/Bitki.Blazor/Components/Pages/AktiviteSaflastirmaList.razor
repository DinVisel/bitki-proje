@page "/aktivitesaflastirma"
@using Bitki.Blazor.Models
@inject HttpClient Http

<h3>Aktivite Saflaştırma Listesi</h3>

@if (items == null)
{
    <p><em>Yükleniyor...</em></p>
}
else
{
    <p>Kayıt Sayısı: @items.Length</p>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Adı</th>
                <th>İngilizce</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in items)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.EnglishName</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private AktiviteSaflastirmaViewModel[]? items;

    protected override async Task OnInitializedAsync()
    {
        items = await Http.GetFromJsonAsync<AktiviteSaflastirmaViewModel[]>("api/aktivitesaflastirma");
    }
}