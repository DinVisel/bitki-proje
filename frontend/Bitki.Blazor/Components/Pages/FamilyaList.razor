@page "/familya"
@using Bitki.Blazor.Models
@inject HttpClient Http

<h1>Familya Listesi</h1>

@if (items == null)
{
    <div class="loading-indicator">
        <div class="spinner-border spinner-border-sm text-secondary" role="status"></div>
        Veriler yükleniyor...
    </div>
}
else
{
    <div class="record-count">Kayıt Sayısı: @items.Length</div>

    <div class="table-responsive">
        <table class="table academic-table">
            <thead>
                <tr>
                    <th class="text-numeric" style="width: 80px;">Id</th>
                    <th>Ad</th>
                    <th>Türkçe Ad</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in items)
                {
                    <tr>
                        <td class="text-numeric text-muted">@item.Id</td>
                        <td class="fw-medium text-primary">@item.Name</td>
                        <td>@item.TurkishName</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

@code {
    private FamilyaViewModel[]? items;

    protected override async Task OnInitializedAsync()
    {
        items = await Http.GetFromJsonAsync<FamilyaViewModel[]>("api/familya");
    }
}