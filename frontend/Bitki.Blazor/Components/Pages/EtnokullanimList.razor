@page "/etnokullanim"
@using Bitki.Blazor.Models
@inject HttpClient Http

<h3>Etnobotanik Kullanım Listesi</h3>

@if (items == null)
{
    <p><em>Yükleniyor...</em></p>
}
else
{
    <p>Kayıt Sayısı: @items.Length</p>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lokalite Id</th>
                <th>Açıklama</th>
                <th>Oluşturma Tarihi</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in items)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.LocalityId</td>
                    <td>@item.Description</td>
                    <td>@item.CreatedDate</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private EtnokullanimViewModel[]? items;

    protected override async Task OnInitializedAsync()
    {
        items = await Http.GetFromJsonAsync<EtnokullanimViewModel[]>("api/etnokullanim");
    }
}